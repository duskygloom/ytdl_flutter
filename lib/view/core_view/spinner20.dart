import 'package:flutter/material.dart';

class Spinner20 extends StatelessWidget {
  const Spinner20({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: 20, height: 20, child: CircularProgressIndicator());
  }
}
